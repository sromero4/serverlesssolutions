from django.apps import AppConfig

class PerfilConjuntoConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Apps.conjunto.perfil'
    label = 'perfil_conjunto'  # etiqueta única
