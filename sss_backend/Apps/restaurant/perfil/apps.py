from django.apps import AppConfig

class PerfilRestaurantConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Apps.restaurant.perfil'
    label = 'perfil_restaurant'  # etiqueta única
