from django.apps import AppConfig


class UsuarioRestaurantConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Apps.restaurant.usuario'
    label = 'usuario_restaurant'  # etiqueta única
